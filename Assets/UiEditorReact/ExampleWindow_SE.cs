using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SEditor
{
    public class ExampleWindow_SE : SE_Window
    {
        // Start is called before the first frame update
        void Start()
        {
            //AddElement()
        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}